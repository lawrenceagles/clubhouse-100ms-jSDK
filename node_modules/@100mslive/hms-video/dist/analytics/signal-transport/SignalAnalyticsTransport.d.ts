import { ISignal } from '../../signal/ISignal';
import { AnalyticsTransport } from '../AnalyticsTransport';
import { LocalStorageEvents } from './LocalStoageEvents';
export declare class SignalAnalyticsTransport extends AnalyticsTransport {
    transportProvider: ISignal;
    failedEvents: LocalStorageEvents;
    constructor(transportProvider: ISignal);
}
