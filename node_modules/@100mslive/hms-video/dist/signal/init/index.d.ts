import { InitConfig } from './models';
export default class InitService {
    static fetchInitConfig(token: string, initEndpoint?: string, region?: string): Promise<InitConfig>;
}
export declare function getUrl(endpoint: string, region?: string): string;
export declare function transformInitConfig(config: any): InitConfig;
