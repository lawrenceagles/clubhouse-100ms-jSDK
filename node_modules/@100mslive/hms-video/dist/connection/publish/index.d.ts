import HMSConnection from '../index';
import { ISignal } from '../../signal/ISignal';
import { IPublishConnectionObserver } from './IPublishConnectionObserver';
import HMSTransport from '../../transport';
import { HMSLocalTrack } from '../../media/tracks';
export default class HMSPublishConnection extends HMSConnection {
    private readonly observer;
    readonly nativeConnection: RTCPeerConnection;
    private readonly transport;
    constructor(signal: ISignal, config: RTCConfiguration, observer: IPublishConnectionObserver, transport: HMSTransport);
    initAfterJoin(): void;
    trackUpdate(track: HMSLocalTrack): void;
}
