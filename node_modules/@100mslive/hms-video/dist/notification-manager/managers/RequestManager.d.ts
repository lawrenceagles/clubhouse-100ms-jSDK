import { HMSUpdateListener } from '../../interfaces';
import { IStore } from '../../sdk/store';
import { RoleChangeRequestParams, TrackUpdateRequestNotification, ChangeTrackMuteStateNotification } from '../HMSNotifications';
/**
 * Handles request from remote peers to change something on the local side. For eg. role change, track mute/unmute.
 */
export declare class RequestManager {
    private store;
    listener?: HMSUpdateListener | undefined;
    constructor(store: IStore, listener?: HMSUpdateListener | undefined);
    handleRoleChangeRequest(notification: RoleChangeRequestParams): void;
    handleTrackUpdateRequest(trackUpdateRequest: TrackUpdateRequestNotification): void;
    handleChangeTrackStateRequest(request: ChangeTrackMuteStateNotification): void;
}
