import { HMSUpdateListener } from '../../interfaces';
import { IStore } from '../../sdk/store';
import { MessageNotification } from '../HMSNotifications';
export declare class BroadcastManager {
    private store;
    listener?: HMSUpdateListener | undefined;
    constructor(store: IStore, listener?: HMSUpdateListener | undefined);
    private get TAG();
    handleBroadcast(messageNotification: MessageNotification): void;
}
