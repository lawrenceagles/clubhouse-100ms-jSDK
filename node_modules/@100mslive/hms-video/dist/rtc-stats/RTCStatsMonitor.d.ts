import HMSConnection from '../connection';
import { TypedEventEmitter } from '../utils/typed-event-emitter';
import { RTCStats } from './RTCStats';
export declare class RTCStatsMonitor extends TypedEventEmitter<{
    RTC_STATS_CHANGE: RTCStats;
}> {
    private readonly connections;
    private readonly interval;
    private isMonitored;
    constructor(connections: HMSConnection[], interval?: number);
    start(): Promise<void>;
    stop(): void;
    private startLoop;
    private handleConnectionsStats;
}
