import InitialSettings from '../interfaces/settings';
import { HMSUpdateListener } from '../interfaces/update-listener';
import ITransport from '../transport/ITransport';
import { IStore } from './store';
export declare type PublishConfig = {
    publishAudio?: boolean;
    publishVideo?: boolean;
};
export default class RoleChangeManager {
    private store;
    private transport;
    private publish;
    private removeAuxillaryTrack;
    private listener?;
    constructor(store: IStore, transport: ITransport, publish: (settings: InitialSettings, publishConfig?: PublishConfig) => void, removeAuxillaryTrack: (trackId: string) => void, listener?: HMSUpdateListener | undefined);
    handleLocalPeerRoleUpdate: (event: any) => Promise<void>;
    private removeLocalTracks;
}
