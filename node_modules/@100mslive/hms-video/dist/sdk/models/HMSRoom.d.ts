import { HMSRoom, HMSRoomType } from '../../interfaces/room';
import { IStore } from '../store/IStore';
export default class Room implements HMSRoom {
    id: string;
    name: string;
    private store;
    shareableLink: string;
    type: HMSRoomType;
    hasWaitingRoom: boolean;
    get localPeer(): import("./peer").HMSLocalPeer;
    get peers(): import("./peer").HMSPeer[];
    constructor(id: string, name: string, store: IStore);
}
