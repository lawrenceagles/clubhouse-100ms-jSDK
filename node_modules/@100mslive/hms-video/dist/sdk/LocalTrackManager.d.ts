import { HMSTrackSettings } from '../media/settings';
import InitialSettings from '../interfaces/settings';
import { HMSLocalTrack } from '../media/tracks';
import { IStore } from './store';
import { IFetchAVTrackOptions } from '../transport/ITransport';
import ITransportObserver from '../transport/ITransportObserver';
import { DeviceManager } from '../device-manager';
export declare class LocalTrackManager {
    private store;
    private observer;
    private deviceManager;
    readonly TAG: string;
    constructor(store: IStore, observer: ITransportObserver, deviceManager: DeviceManager);
    getTracksToPublish(initialSettings: InitialSettings): Promise<HMSLocalTrack[]>;
    getLocalTracks(fetchTrackOptions: IFetchAVTrackOptions | undefined, settings: HMSTrackSettings): Promise<Array<HMSLocalTrack>>;
    private getNativeLocalTracks;
    private getEmptyVideoTrack;
    private getEmptyAudioTrack;
    private getAVTracks;
    private getTrackSettings;
}
